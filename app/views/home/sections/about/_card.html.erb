<%# locals: (title:, content:) %>
<div class="glass-card border-neon p-5 md:p-8 reveal-glow" data-scroll-reveal-target="item">
  <h3 class="text-lg md:text-xl font-semibold text-white mb-3 md:mb-4 flex items-center gap-2">
    <span class="text-neon-500">&#9679;</span>
    <%= title %>
  </h3>
  <%= content %>
</div>
